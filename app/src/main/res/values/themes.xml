<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.FajrBuddy3" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Main Colors -->
        <item name="colorPrimary">@color/midnight_bg</item>
        <item name="colorPrimaryVariant">@color/surface_card</item>
        <item name="colorOnPrimary">@color/text_primary</item>

        <!-- Secondary/Accent Colors (UPDATED TO APP_ACCENT) -->
        <item name="colorSecondary">@color/app_accent</item>
        <item name="colorSecondaryVariant">@color/app_accent_pressed</item>
        <item name="colorOnSecondary">@color/midnight_bg</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/midnight_bg</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Standard Background -->
        <item name="android:windowBackground">@color/midnight_bg</item>
    </style>
</resources>